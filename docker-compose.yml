services:
  client:
    build: ./client

    ports:
      - "5173:5173"

    environment:
      VITE_WS_URL: ws://localhost:8080

    depends_on:
      - server

  server:
    build: ./server

    ports:
      - "8080:8080"
